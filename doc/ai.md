

# 请遵守以下AI守则
- 深呼吸,然后一步一步地解决问题,解决一个小问题测试OK后再继续
- 我没有手指做这件事,直接修改文件,不要发大段代码在对话框 
- 做最小必要修改,能改一行的不要改二行,不要去修改任何和当前任务不相关的代码,特别不允许删除之前OK了的代码
- 缺少参数缺少信息应该主动询问,不允许为了编译通过而乱改代码
- 当前运行是正常的,请不要无谓修改类名 函数名等各种定义 尽量少修改当前逻辑和功能 不允许删除注释 适当加注释或修改为适合的
- 调试时出错把关键日志截取有用信息发给我 不要发整条



# 目标
- 我想搭建一个SEO系统  通过长尾关键字来赚谷歌广告和用户付的会员费 我们这套系统搭在K8S上面 NGINX区分不同的域名到不同的目录
- 后端NODEJS KOA也是在K8S上面 容器化
- 我们要针对不同的关键词 做不同的域名 比如gameinfo.com shopgo.com等等（域名会用很多哪些后辍最便宜）
- 有部分内容我们可能 通过iframe直接把目标网站嵌套进来 还有一部分通过提供攻略 说明等来做 提供下载或链接到目标网站去
- 还有一部分可能是SAAS服务 我们可以通过收取会员费获利

# 问题 一个个来
- npx create-next-app@latest seo-site --typescript --tailwind --eslint --no-src-dir --app --no-turbo 之前通过这个命令创建的 ROBOT.txt sitemap.xml都还没有 还有哪些通用的文件需要的 目录如何组织 现在生成的这些page.tsx 这些是SEO友好的吗 
- 先弄好一个域名的 托管到cloudflare DEMO
- 域名会用很多哪些后辍最便宜
- 如何生成静态站
- 如何从cloudflare pages调用Workers API 添加动态内容




