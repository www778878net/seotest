#!/bin/sh
# 在推送之前执行构建命令
echo "Running npm run build before pushing..."
npm run build

# 如果构建失败，终止推送
if [ $? -ne 0 ]; then
  echo "Build failed, aborting push."
  exit 1
fi

# 继续执行推送
exit 0